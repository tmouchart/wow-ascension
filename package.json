{
  "name": "wow-ascension",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "npm --prefix weakauras/web run dev",
    "dev:server": "node weakauras/server/server.mjs",
    "build": "npm --prefix weakauras/web run build",
    "preview": "npm --prefix weakauras/web run preview"
  }
}
